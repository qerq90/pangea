-- +goose Up
-- Экран лавки Ришелье (3 предмета со сравнением с надетым + клавиатура) уходил в ВК
-- через GET: в percent-encoding строка запроса раздувалась до ~7-8 КБ и упиралась в
-- лимит на стороне ВК. Ответ 414 приходил HTML-ом, а не JSON, и молча считался
-- успехом — герой переезжал в Merchant без единого доставленного экрана, а любое
-- следующее нажатие рисовало тот же слишком длинный экран. Отправка переведена на
-- POST, не-2xx и не-JSON ответы теперь ошибка (см. VkApi). Возвращаем застрявшего
-- игрока на торговую площадь.
UPDATE heroes
   SET state = 'MarketSquare'
 WHERE state = 'Merchant'
   AND user_id = (SELECT id FROM users WHERE vk_id = '89655198');

-- +goose Down
SELECT 1;
